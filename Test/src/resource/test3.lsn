#include "resource\test2"

fn foo(a:int, b:double)
{
	let c = a - b;
	if(a < b)
	{
		let mut d = a * 3;
		say "Hello";
		if(d < b)
		{
			let mut e = d + a;
			say "Hello"*e;
		}
	}
	let mut f = a /(b+1);
}
